/**
 * Create a place to hold our magic strings
 * We use a class so we can get intellisense
 * As an alternative to using static properties we could also export an instance of the class
 */

export class Constants {
    static ALLOW_INITIAL_SEARCH = false;
    /** Default Page Size when searching GitHub */
    static PAGE_SIZE = 3;
}